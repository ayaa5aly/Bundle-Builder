/**
 * Placeholder — the "Your security system" review panel is wired up
 * in a later step. Sticky positioning on desktop only; on mobile it
 * flows naturally below the builder.
 */
export function ReviewColumn() {
  return (
    <aside
      aria-label="Order summary"
      className="rounded-2xl bg-[var(--color-panel)] p-6 text-slate-400 lg:sticky lg:top-8"
    >
      Review panel goes here
    </aside>
  );
}
